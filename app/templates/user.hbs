{{#if (and (eq model.currentUser.currentUser.permissions 'admin') (eq model.user model.currentUser.currentUser))}}

  {{admin-wrapper user=model.user users=model.users addNewGame="addNewGame" addAdmin="addAdmin" deleteUser="deleteUser"}}
{{else}}
  {{user-wrapper user=model.user changePassword="changePassword"}}
{{/if}}
