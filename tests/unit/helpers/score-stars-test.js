import { scoreStars } from 'game-reviews/helpers/score-stars';
import { module, test } from 'qunit';

module('Unit | Helper | score stars');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scoreStars([42]);
  assert.ok(result);
});
